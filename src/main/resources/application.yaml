spring:
  application:
    name: accesscontrol

  h2:
    console:
      enable: true
      path: /h2

  datasource:
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

